# ============================================================
# JPQuiz 环境变量模板
# 用法：cp .env.example .env.local 并填入实际值
# ============================================================

# ── Cron Secret ───────────────────────────────────────────────
# 用于 /api/cron/check-expiry 端点的鉴权（会员到期自动降级）。
# 必须设置才能调用 cron 端点；否则返回 503。
# 生成方法：openssl rand -hex 32
CRON_SECRET=

# ── 认证签名密钥（推荐生产环境设置） ───────────────────────────
# 用于「忘记密码」流程的临时 reset token HMAC 签名（5 分钟有效）。
# 不设置时进程启动会自动生成进程内随机密钥；多实例部署 / 希望重启
# 后已发出的 token 仍能续用时需要显式设置。
# 生成方法：openssl rand -hex 32
JPQUIZ_AUTH_SECRET=

# ── 备注 ──────────────────────────────────────────────────────
# 平台 AI 模型配置（apiKey/baseUrl/model）不在这里 —— 走管理员后台
# /admin/settings 页面写入 SQLite 的 admin_model_config 表。
# 这样可以在不重启服务的情况下切换 provider。
